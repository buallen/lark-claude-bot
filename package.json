{
  "name": "lark-bridge",
  "version": "1.0.0",
  "description": "Generic Lark messaging bridge — auto-reply via Claude or send-only HTTP API for any app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "@larksuiteoapi/node-sdk": "^1.42.0"
  }
}
